<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cashflowapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CashFlowApp">
        <activity android:name=".LiabilitiesPage"></activity>
        <activity android:name=".AssetsPage" />
        <activity android:name=".ExpensesPage" />
        <activity android:name=".IncomePage" />
        <activity android:name=".MainPage" />
        <activity android:name=".Occupation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>